program TestFailures;
var
  i: integer;
  s: string;
  b: boolean;

procedure Teste(var num: integer);
begin
  num := num + 1;
end;

begin
  i := 'texto';      { ERRO: Atribuição de tipo incompatível }
  s := i;            { ERRO: Atribuição de tipo incompatível }
  b := i + 1;        { ERRO: Operação inválida para os tipos }

  Teste(5);          { ERRO: Passando literal para um parâmetro VAR }
  
  writeln(variavel_nao_declarada); { ERRO: Variável não existe }
end.
