{ Code generated by AI agent based on the definition of BNF - I made minor changes on the code btw }
program ComplexExample;

type
    IntArray = array[1..10] of integer;
    CharSet = set of char;
    Person = record
        name: string;
        age: integer;
        scores: IntArray;
    end;
    Max = integer;
    
var
    people: array[1..5] of Person;
    vowels: CharSet;
    i: integer;

procedure InitializePerson(var p: Person; name: string; age: integer);
begin
    p.name := name;
    p.age := age;
    for i := 1 to 10 do
        p.scores[i] := 0;
end;

function IsVowel(c: char): boolean;
begin
    IsVowel := c in vowels;
end;

begin
    vowels := ['a', 'e', 'i', 'o', 'u'];
    
    InitializePerson(people[1], 'João', 25);
    people[1].scores[1] := 85;
    people[1].scores[2] := 90;
    
    if IsVowel('a') then
        writeln('a é uma vogal');
        
    for i := 1 to 10 do
        if people[1].scores[i] > 0 then
            writeln('Score ', i, ': ', people[1].scores[i]);
end.
