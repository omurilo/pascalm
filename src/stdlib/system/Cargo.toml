[package]
name = "system"
version = "0.1.0"
edition = "2021"

[dependencies]

[lib]
crate-type = ["staticlib"]

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
strip = true
panic = "abort"
